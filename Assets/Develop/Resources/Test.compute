#pragma kernel CSMain

struct Test
{
	float x;
	float y;
	float z;
};
struct Test2
{
	Test t;
	float w;
};

cbuffer Hoge
{
	float fuga[2];
};

RWStructuredBuffer<Test2> _test;

[numthreads(10,10,10)]
void CSMain (uint3 threadID : SV_DispatchThreadID)
{
	float3 hoge = float3(1, 0, 0);
	
	_test[threadID.x].t.x = 1;
	_test[threadID.x].t.y = 2;
	_test[threadID.x].t.z = 3;
	_test[threadID.x].w = 4;
}
